def simpleInterest(p,r,t): # p = principle, r = rate, t = time
    print(f"Value of your simple Interest isn {(p*r*t)/100}") # display the result

simpleInterest(1000,1,10) # calling the function with arguments